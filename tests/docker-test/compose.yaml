version: "3"

services:
    server:
        build: ./server
        ports:
            - 10243:10243
    client:
        build: ./client
        network_mode: host
        depends_on:
            - server
